<?php

class JudgesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('judges')->truncate();

		$judges = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('judges')->insert($judges);
	}

}
